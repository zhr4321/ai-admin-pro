---
description: MSW 本地 Mock 与 handler 编写约定
globs: src/mocks/**/*
alwaysApply: false
---

# MSW Mock 规范

> 接口封装与页面调用方式见 `api-request.mdc`。

## 基本原则

- Mock 仅存在于 `src/mocks/`，**禁止**在 `request.ts` 或组件内写 mock 数据
- 开发环境由 `main.ts` 启动 Worker；生产环境不启动 MSW
- handler 拦截真实 HTTP 路径，与 axios `baseURL + url` 一致

## 目录结构

```
src/mocks/
├── handlers/
│   ├── auth.ts
│   ├── dashboard.ts
│   └── index.ts      # 聚合导出
├── browser.ts        # setupWorker
└── index.ts          # startMockWorker()
```

## 新增 Mock 流程

1. `src/types/` 定义类型
2. `src/mocks/handlers/<模块>.ts` 添加 handler
3. `handlers/index.ts` 注册
4. `src/api/<模块>.ts` 封装 API
5. `npm run dev` 验证控制台无未匹配请求警告

## Handler 写法

```typescript
import { http, HttpResponse } from 'msw'

http.post('/api/auth/login', async ({ request }) => {
  const body = await request.json()
  return HttpResponse.json({
    code: 0,
    message: 'success',
    data: { /* ... */ },
  })
})
```

- 路径写完整：`/api/xxx`（因 `baseURL` 为 `/api`）
- 成功：`code: 0` + `data`
- 鉴权失败：HTTP 401 + `code !== 0`
- 按业务模块拆分文件，避免单文件过大

## 切换真实后端

修改 `VITE_APP_BASE_API` 指向真实域名；关闭或移除开发环境 `startMockWorker()` 即可。
